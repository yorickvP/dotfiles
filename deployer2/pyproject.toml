[project]
name = "deployer2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fire>=0.7.0",
    "pydantic>=2.10.6",
]

[project.scripts]
ydeployer2 = "ydeployer.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

# [tool.ruff.lint]
# extend-select = ["ALL"]
# ignore = [ "ANN", "D", "T201", "EM101" ]
